package hitori.mainpackage;

import static org.junit.jupiter.api.Assertions.*;

class Test {

	@org.junit.jupiter.api.Test
    public void multiplicationOfZeroIntegersShouldReturnZero() {
        GridPane tester = new GridPane(2); 
        boolean[][] clicked = new boolean[2][2];
        /*for (int i = 0; i < 2; i++) {
			for (int j = 0; j < 2; j++) {
				clicked[i][j] = true;
			}
		}*/
        // assert statements
       // assertEquals(0, tester.checkTouch(1,1,clicked), "10 x 0 must be 0");
    }
}
